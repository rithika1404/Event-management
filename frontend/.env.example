# Copy this file to .env.local for local development
# In production (Vercel), set VITE_API_URL as an Environment Variable in your project settings

# URL to the deployed backend API (must include /api at the end)
VITE_API_URL=https://your-backend.vercel.app/api
